<?xml version="1.0" encoding="utf-8"?>
<!-- Story 7.3: Saved Location Picker Dialog Layout -->
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="24dp">

    <!-- Dialog Title -->
    <TextView
        android:id="@+id/dialogTitleTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/select_saved_location_title"
        android:textAppearance="?attr/textAppearanceHeadlineSmall"
        android:textColor="?attr/colorOnSurface"
        android:contentDescription="@string/select_saved_location_title"
        android:importantForAccessibility="yes"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Saved Locations RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/savedLocationsRecyclerView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="200dp"
        android:maxHeight="400dp"
        android:contentDescription="@string/saved_locations_list_description"
        android:importantForAccessibility="yes"
        app:layout_constraintTop_toBottomOf="@id/dialogTitleTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        tools:listitem="@layout/item_saved_location_picker"
        tools:itemCount="3" />

    <!-- Empty State (hidden when locations exist) -->
    <TextView
        android:id="@+id/emptyStateTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/no_saved_locations"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:gravity="center"
        android:padding="48dp"
        android:contentDescription="@string/no_saved_locations_announcement"
        android:importantForAccessibility="yes"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/dialogTitleTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>
